# TuskerSquad — root/dev requirements
# These are for LOCAL development only.
# Docker services use services/langgraph_api/requirements.txt
# and apps/backend/requirements.txt

fastapi>=0.111.0
uvicorn[standard]>=0.30.0
httpx>=0.27.0
pyyaml>=6.0.0
psutil>=5.9.0
pytest>=8.0.0
pytest-asyncio>=0.23.0
